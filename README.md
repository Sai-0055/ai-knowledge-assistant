# AI Knowledge Assistant

Monorepo with `backend` (Node.js) and `frontend` (Vite + React + TypeScript).

Quick start:

```powershell
cd backend
npm install
npm start

cd ..\frontend
npm install
npm run dev
```

Create a GitHub repo and push following the instructions in the project root.
